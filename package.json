{
  "name": "spring-angular-jwt",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "ng": "ng",
    "dev": "concurrently \"npm run client\" \"npm run start:backend\"",
    "client": "npm install && cd client && npm install &&ng serve --o  ",
    "start:backend": "npm install && cd backend && mvn install && mvn spring-boot:run"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "spring-angular-jwt": "file:"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
